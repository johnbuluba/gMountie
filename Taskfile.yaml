version: '3'

tasks:
  gen-grpc:
    cmds:
      - | 
        protoc --go_out=. --go-grpc_out=. api/proto/*.proto

  test:
    cmds:
      - go test -p=1 ./... $TEST_EXTRA_A

  build:server:
    cmds:
      - go build -o out/server cmd/server/main.go

